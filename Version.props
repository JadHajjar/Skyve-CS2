<Project>
	<PropertyGroup Condition="'$(Configuration)' == 'Stable'">
		<AssemblyVersion>1.0.0.0</AssemblyVersion>
	</PropertyGroup>

	<PropertyGroup Condition="'$(Configuration)' != 'Stable'">
		<AssemblyVersion>1.0.0.0</AssemblyVersion>
	</PropertyGroup>

	<PropertyGroup>
		<FileVersion>1.0</FileVersion>
		<DefineConstants>CS2</DefineConstants>
	</PropertyGroup>
</Project>